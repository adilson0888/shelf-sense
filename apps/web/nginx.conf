# Serves the Vite-built static SPA. try_files falls back to index.html so
# client-side routing (any non-file path) still resolves instead of 404ing.
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
