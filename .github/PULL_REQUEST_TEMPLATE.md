## Type

- [ ] New feature / behavior change
- [ ] Bug fix
- [ ] Chore (deps, CI, docs, typos, etc.)

## Spec

<!--
Required only for "New feature / behavior change" — link the spec in specs/
this PR implements or updates. See CONTRIBUTING.md's "How contributions are
evaluated" section: for a new feature, the spec is what gets evaluated, not
the code. A code-only PR for a new feature with no linked spec gets closed.

Not required for a bug fix (unless it also corrects the existing spec) or a
chore — leave this section out.
-->

Spec:

## Summary

<!-- What does this change, and why? -->

## Checklist

- [ ] `npm run typecheck` passes
- [ ] For a new feature/behavior change: the linked spec's acceptance criteria are met
